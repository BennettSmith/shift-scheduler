// Domain layer for Cloud Functions.
// Place entities, value objects, repository interfaces,
// and business rules here.

export interface DomainPlaceholder {
  readonly ok: boolean;
}

export const domainPlaceholder: DomainPlaceholder = {ok: true};


